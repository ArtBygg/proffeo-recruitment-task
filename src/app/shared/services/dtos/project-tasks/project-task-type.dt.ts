export interface TaskTypeDTO {
  id: string;
  code: string;
  name: string;
  isDescriptionCommentingEnabled: boolean;
  // allowAccept: boolean;
  // allowReject: boolean;
  // allowRequestChanges: boolean;
  // allowReadyForReview: boolean;
}
